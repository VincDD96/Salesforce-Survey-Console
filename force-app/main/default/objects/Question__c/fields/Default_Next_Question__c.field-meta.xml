<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Default_Next_Question__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Fallback graph edge used when no Answer_Option carries the next-question for this node. Required for Free Text / Scale / Date questions (which have no options) and for Multi Choice questions where selected options route divergently. Empty = terminal node.</description>
    <externalId>false</externalId>
    <label>Default Next Question</label>
    <referenceTo>Question__c</referenceTo>
    <relationshipLabel>Default-Routed From</relationshipLabel>
    <relationshipName>Default_Routed_From</relationshipName>
    <required>false</required>
    <trackHistory>true</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
