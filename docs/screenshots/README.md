# Screenshots checklist

Referenced by the root `README.md` / `README.it.md`. Add these files here with these exact names (PNG, ~1200–1600px wide is plenty):

| File                    | What to capture                                                                                                                           |
| ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| `survey-runner.png`     | The `surveyRunner` component mid-compilation, ideally with a custom theme applied — progress bar visible, a branching question on screen. |
| `survey-author.png`     | The Survey Author graph editor canvas with a few branching nodes, inspector panel open on a selected question.                            |
| `survey-experience.png` | The Survey Experience theme editor: configuration panel on one side, live preview on the other.                                           |
| `export.png`            | The "Esporta risposte" button in Survey Author, or the exported CSV opened in a spreadsheet app.                                          |

This file itself is not linked from the READMEs — delete it once the screenshots are in place, or leave it as a maintenance note for future updates.
